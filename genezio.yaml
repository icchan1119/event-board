frontend:
    path: .
    publish: build
    scripts:
        deploy:
            - npm install --legacy-peer-deps
            - npm run build
    subdomain: event-board
name: event-board
region: us-east-1
yamlVersion: 2
